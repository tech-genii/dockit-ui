const cli = require('commander');
const chalk = require('chalk');

cli
    .command('develop')
    .alias('v')
    .description("This commands helps you run a development environment")
    .action(args => {

    });